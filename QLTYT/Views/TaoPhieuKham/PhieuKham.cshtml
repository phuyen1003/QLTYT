@model QLTYT.ViewModel.MutipleData2
@{
    ViewBag.Title = "PhieuKham";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="~/Content/Admin.css" rel="stylesheet">
<link rel="stylesheet" href="~/css/themify-icons/themify-icons.css">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.3/jquery.min.js"></script>
<div class="table-title">
  <h2>Quản lý thông tin phiếu khám</h2>
  <br />
  <br />
  <br />

  <button class="nv btn " type="button" data-toggle="tooltip" data-placement="top"
          title="Thêm ">

    <span?>
      @Html.ActionLink("Thêm", "TaoPhieuKham")
    </span?>

  </button>
  <div class="seach">
    <form>
      <input type="text" id="Search" placeholder="Tìm kiếm..." class="input"><a href=""><i class="icon ti-search"></i></a>
    </form>
  </div>
  <table class="table-responsive text-nowrap table-bordered" id="myTable">
    <thead>
      <tr class="ex">

        <th>
          @Html.DisplayName("Số phiếu")
        </th>
        <th>
          @Html.DisplayName("Tên nhân viên")
        </th>
        <th>
          @Html.DisplayName("Tên phòng")
        </th>
        <th>
          @Html.DisplayName("Loại phòng")
        </th>
        <th>
          @Html.DisplayName("Tên bệnh nhân")
        </th>
        <th>
          @Html.DisplayName("Nhóm bệnh nhân")
        </th>
        <th>
          @Html.DisplayName("Tuần tuổi")
        </th>
        <th>
          @Html.DisplayName("Ngày tạo")
        </th>

        <th></th>
        <th></th>



      </tr>

      @foreach (var item in Model.phieuKhams)
      {
<tr class="Search" id="row_@item.IdSoPhieu">
  <td>
    @Html.DisplayFor(modelItem => item.IdSoPhieu)
  </td>
  <td>
    @Html.DisplayFor(modelItem => item.NhanVien.HoTen)
  </td>

  <td>
    @Html.DisplayFor(modelItem => item.Phong.TenPhong)
  </td>
  <td>
    @Html.DisplayFor(modelItem => item.Phong.LoaiPhong.TenLoaiPhong)
  </td>
  <td>
    @Html.DisplayFor(modelItem => item.BenhNhan.HoTen)
  </td>
  <td>
    @Html.DisplayFor(modelItem => item.BenhNhan.NhomBenhNhan.TenNhom)
  </td>
  <td>
    @Html.DisplayFor(modelItem => item.ThongTinThaiKy.TuanTuoi)
  </td>
  <td>
    @Html.DisplayFor(modelItem => item.NgayTao)
  </td>


  <td>
    @Html.ActionLink(" ", "SuaDoiPhieuKham", new { id = item.IdSoPhieu }, new { @class = "far fa-edit" })
  </td>

  @*<td>
        @Html.ActionLink(" ", "Delete", new { id = item.IdVacXin }, new { @class = "fas fa-trash-alt" })
    </td>*@
  <td class="btn-delete js-delete">
    <a class="delete-link" data-id="@item.IdSoPhieu" data-name="@item.BenhNhan.HoTen">
      <i class="fas fa-trash-alt"></i>
    </a>
  </td>
</tr>}

  </table>








  <style>
    .confirmation-box {
      position: fixed;
      z-index: 9999;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 400px;
      background-color: #fff;
      padding: 20px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
      text-align: center;
    }

      .confirmation-box h3 {
        font-size: 20px;
        margin-bottom: 20px;
      }

      .confirmation-box p {
        font-size: 16px;
        margin-bottom: 20px;
      }

      .confirmation-box .button-group {
        display: flex;
        justify-content: center;
      }

        .confirmation-box .button-group button {
          margin: 0 10px;
          padding: 10px 20px;
          font-size: 16px;
        }

          .confirmation-box .button-group button.yes {
            background-color: #e74c3c;
            color: #fff;
            border: none;
          }

          .confirmation-box .button-group button.no {
            background-color: #ccc;
            color: #333;
            border: none;
          }
  </style>

  <div id="loader" style="display:none;">
    <img src="~/image/loader.gif" alt="Loading..." />
  </div>

  <script>
    $(document).ready(function () {
      function Contains(text_one, text_two) {
        if (text_one.indexOf(text_two) != -1)
          return true;
      }
      $("#Search").keyup(function () {
        var searchText = $("#Search").val().toLowerCase();
        $(".Search").each(function () {
          if (!Contains($(this).text().toLowerCase(), searchText)) {
            $(this).hide();
          }
          else {
            $(this).show();
          }
        });
      });
    });
  </script>

  <script>
    $(document).ready(function () {
      $('.delete-link').click(function (e) {
        e.preventDefault();
        var id = $(this).data('id');
        var name = $(this).data('name');
        var confirmationBoxHtml = '<div class="confirmation-box">' +
          '<h3>Xóa phiếu khám</h3>' +
          '<p>Bạn có chắc chắn muốn xóa phiếu khám của <strong>' + name + '</strong>?</p>' +
          '<div class="button-group">' +
          '<button class="yes" data-id="' + id + '">Có</button>' +
          '<button class="no">Không</button>' +
          '</div>' +
          '</div>';
        $('body').append(confirmationBoxHtml);
        $('.yes').click(function () {
          var id = $(this).data('id');
          deleteVacXin(id);
          $('.confirmation-box').remove();
          $('#loader').show();
        });
        $('.no').click(function () {
          $('.confirmation-box').remove();
        });
      });
    });

    function deleteVacXin(id) {

      // Hiển thị loader
      $('#loader').show();

      // Xóa bệnh
      $.ajax({
        url: '/TaoPhieuKham/XoaPhieuKham/' + id,
        type: 'POST',
        success: function (result) {
          // Ẩn loader
          $('#loader').hide();
          // Xóa dòng thông tin bệnh trong bảng
          $('#row_' + id).remove();
        },
        error: function () {
          // Ẩn loader
          $('#loader').hide();
          alert('Đã xảy ra lỗi khi xóa phiếu khám!');
        }
      });

    }
  </script>





</div>







